# import Poll, Option, Vote from "./generated/prisma.graphql"

input OptionInput {
  name: String!
}

type Query {
  polls: [Poll!]!
}

type Mutation {
  postPoll(name: String!, options: [OptionInput!]!): Poll!
  vote(pollId: String!): Vote!
}

