type Poll {
  id: ID! @unique
  name: String!
  options: [Option!]!
}

type Option {
  id: ID! @unique
  name: String!
  votes: [Vote!]!
  poll: Poll!
}

type Vote {
  id: ID! @unique
  option: Option!
}
